- test config set
- test every combination of configuration against behavior

- make 

docs:

'cache' and 'persistedCache' should not be called repeatedly with the same arguments 
(you probably want to use it at the top level)
create the cached function once and call *that* multiple times